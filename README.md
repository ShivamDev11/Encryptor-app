# Encryptor app
🔐 Secure Encryptor – Laravel Encryption & Decryption App

A modern, authentication-style Encryption & Decryption web application built using Laravel.
The app allows users to securely encrypt and decrypt messages using Laravel’s built-in cryptography system, presented with a premium animated UI.

✨ Features

🔒 Encrypt any text message securely

🔓 Decrypt encrypted messages instantly

🎨 Premium animated gradient background

🧭 Auth-style sliding panels (Encrypt ↔ Decrypt)

📱 Fully responsive (Desktop, Tablet, Mobile)

🧊 Glassmorphism UI design

⚡ Smooth UX with animations

🛡️ Uses Laravel’s AES-256 encryption

🛠 Tech Stack

Backend: Laravel (PHP)

Frontend: Blade, HTML, CSS, Bootstrap 5

Encryption: Laravel Crypt (AES-256-CBC)

Styling: Custom CSS + Bootstrap

Animation: CSS Keyframes (no JS libraries)

📂 Project Structure (Important Files)
encryptor_app/
│
├── app/
│   └── Http/
│       └── Controllers/
│           └── CryptoController.php
│
├── resources/
│   └── views/
│       └── crypto.blade.php
│
├── routes/
│   └── web.php
│
├── .env
├── README.md
└── artisan

⚙️ Installation & Setup
1️⃣ Clone the repository
git clone https://github.com/your-username/secure-encryptor.git
cd secure-encryptor

2️⃣ Install dependencies
composer install

3️⃣ Create environment file
cp .env.example .env

4️⃣ Generate application key
php artisan key:generate

5️⃣ Run the application
php -S localhost:8000 -t public


Open in browser:

http://localhost:8000

🚀 How It Works
🔐 Encryption Flow

User enters a plain text message

Laravel encrypts the message using AES-256

Encrypted output is displayed securely

🔓 Decryption Flow

User pastes encrypted text

Laravel decrypts it using the same app key

Original message is restored

⚠️ Decryption works only if the encrypted text is valid and generated by the same app key.

🎨 UI & UX Highlights

Auth-style left/right panels

Sliding panel navigation

Animated premium gradient background

Glass-effect result containers

Responsive layout for all screen sizes

🔑 Security Notes

Uses Laravel’s Crypt facade

Encryption standard: AES-256-CBC

Secret key is stored securely in .env

No plain text data is stored in the database

🧠 Learning Outcomes

Laravel MVC architecture

Encryption vs Decryption concepts

Secure data handling

UI/UX design with CSS animations

Handling UI state with session
